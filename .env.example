# Google Apps Script Web App URL for RSVP submissions.
# Copy this file to `.env.local` and paste your deployment URL (…/exec).
# See google-apps-script/Code.gs for the setup steps.
# If left empty, RSVPs are saved locally in the browser as a fallback.
NEXT_PUBLIC_RSVP_ENDPOINT=
